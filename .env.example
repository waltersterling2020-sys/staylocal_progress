# StayLocal local development database settings
# Keep real credentials out of Git and configure these in Apache/PHP or hosting.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=staylocal
DB_USER=root
DB_PASS=
